{
  "name": "@erilog/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'web build deferred - VISUAL-REFERENCE-REQUIRED'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@erilog/reconcile": "workspace:*",
    "@erilog/schemas": "workspace:*",
    "@erilog/crypto": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "3.2.3"
  }
}
