{
  "name": "erilog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:integration": "echo 'Integration tests require PostgreSQL — run docker compose up first'",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run typecheck",
    "demo:headless": "tsx scripts/demo-headless.ts",
    "db:setup": "docker compose exec -T db psql -v ON_ERROR_STOP=1 -U erilog -d erilog < drizzle/0001_schema.sql"
  },
  "devDependencies": {
    "turbo": "2.5.4",
    "tsx": "4.19.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
