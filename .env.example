# Database
DATABASE_URL="postgresql://erilog:erilog_dev@localhost:5432/erilog"

# Ed25519 signing key (hex-encoded private key, 64 hex chars)
# Generate with: pnpm tsx scripts/generate-keys.ts
ERILOG_SIGNING_PRIVATE_KEY="<generate-with-scripts/generate-keys.ts>"

# Server
NODE_ENV="development"
PORT="3000"
